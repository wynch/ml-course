[project]
name = "mlcourse-04-attention"
version = "0.1.0"
description = "Module 04 — self-attention and a small transformer built from scratch."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "torch>=2.13.0",
]
