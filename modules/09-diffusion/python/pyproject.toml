[project]
name = "mlcourse-09-diffusion"
version = "0.1.0"
description = "Module 09 — Diffusion: from a 2D point cloud to DDPM images to the diffusers library."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "datasets>=5",
    "diffusers>=0.39.0",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "pillow>=12.3.0",
    "torch>=2.13.0",
]
