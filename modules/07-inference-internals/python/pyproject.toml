[project]
name = "mlcourse-07-inference"
version = "0.1.0"
description = "Module 07 — KV caching, quantization, and efficient autoregressive inference."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "pytest>=9.1.1",
    "torch>=2.13.0",
]
