[project]
name = "mlcourse-01-autograd"
version = "0.1.0"
description = "Module 01 — a tiny reverse-mode autodiff engine built from scratch."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphviz>=0.21",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
    "scikit-learn>=1.9.0",
]
