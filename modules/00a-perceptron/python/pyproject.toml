[project]
name = "mlcourse-00a-perceptron"
version = "0.1.0"
description = "Module 00a — the perceptron, its mistake bound, least squares, and the XOR wall."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "scikit-learn>=1.9.0",
]
